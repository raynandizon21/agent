# dependencies / build output
node_modules/
dist/

# secrets — never commit real env files or Telegram sessions
.env
.env.*
!.env.example
*.session
*.session-journal

# logs / os
*.log
*.log.*
.DS_Store
backup_before_rename.sql
