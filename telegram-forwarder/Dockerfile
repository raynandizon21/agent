FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY *.py ./

ENV PYTHONUNBUFFERED=1

# Persist the session file and logs outside the container (see docker-compose.yml).
# SESSION_NAME / LOG_FILE are pointed at /app/data via the environment.
CMD ["python", "main.py"]
